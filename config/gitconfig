[user]
    email = me@tdem.in
    name = Timur Demin
    signingkey = 9DCB446EF5B024CFE88583029EDF3F9D9286FA20
[core]
    editor = nvim
    autocrlf = false
[commit]
    gpgsign = true
[credential]
    helper = manager
[tag]
    autosign = true
[alias]
    stage = diff --staged
    br = branch
    co = checkout
    st = status
    slog = log --oneline
    lg = log --color --graph --abbrev-commit --date=relative --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
    ignore-for-now = update-index --assume-unchanged
[pull]
    ff = only
[rebase]
    autoStash = true
[init]
    defaultBranch = master
